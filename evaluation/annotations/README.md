# Annotation Tasks

This folder holds human annotations for the evaluation study.

Use `annotation_template.json` as a starting point and save one file per PR.
The `pilot_tasks.json` file lists the current assignment queue.

When two or more annotators finish a PR, keep all raw labels and resolve disagreements in a separate adjudication step.
